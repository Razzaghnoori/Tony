How to install dependencies:
    $virtualenv venv
    $source venv/bin/activate
    $pip install -r requirements.txt

The initialize.py file is responsible for creating necessary models for the entire program to work.
Please first, run this file. Note that this may take a while. Then run main.py to start the program.